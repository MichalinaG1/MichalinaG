using System;

namespace zadanie5
{
    class Program
    {
        static void Main(string[] args)
        {
            //Napisać program wczytujący współrzędne punktu P=(x,y) i wypisujący na ekranie informację,
            // do której ćwiartki ten punkt należy, czy też informację, że leży on na osi OX i/lub osi OY.

            int x, y;
            Console.Write("podaj wspolrzedna x: ");
            x = Convert.ToInt32(Console.ReadLine());
            Console.Write("podaj wspolrzedna y: ");
            y = Convert.ToInt32(Console.ReadLine());

            if (y==0)
                Console.WriteLine("punkt lezy na osi OX");
            if (x==0)
                Console.WriteLine("punkt lezy na osi OY");
            if (x>0 && y>0)
                Console.WriteLine("punkt nalezy do I cwiartki");
            if (x<0 && y>0)
                Console.WriteLine("punkt nalezy do II cwiartki");
            if (x<0 && y<0)
                Console.WriteLine("punkt nalezy do III cwiartki");
            if (x>0 && y<0)
                Console.WriteLine("punkt nalezy do IV cwiartki");
            Console.ReadKey(true);
        }
    }
}
